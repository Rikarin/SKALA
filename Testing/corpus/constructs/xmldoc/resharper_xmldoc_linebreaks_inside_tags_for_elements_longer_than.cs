class LinebreaksInsideTagsForElementsLongerThan {
    /// <summary>Content long enough that it passes any threshold worth naming and long enough that it will not fit on the line it starts on either.</summary>
    void M() { }
}
