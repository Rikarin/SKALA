class WrapTagsAndPi {
    /// <summary>Some prose that runs on for long enough that the inline element which follows it cannot stay on the same line <see cref="System.String" /> as written.</summary>
    void M() { }
}
