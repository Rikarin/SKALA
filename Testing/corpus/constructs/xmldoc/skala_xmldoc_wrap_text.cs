class WrapText {
    /// <summary>Ordinary prose, carrying no elements at all, written at enough length that it cannot fit inside the configured column limit and must therefore be re-flowed.</summary>
    void M() { }
}
