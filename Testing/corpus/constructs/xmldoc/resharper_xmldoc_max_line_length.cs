class MaxLineLength {
    /// <summary>A summary written at enough length that it cannot possibly fit inside the configured column limit, so that the column itself is what decides where the break lands.</summary>
    void M() { }
}
