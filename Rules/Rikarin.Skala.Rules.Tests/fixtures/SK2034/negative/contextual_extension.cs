// `extension` is contextual too, and the extension-block work is exactly why it must stay legal.
static class Helpers {
    public static int @extension;
    public static int @scoped;
    public static int @required;
    public static int @file;
    public static int @args;
    public static int @nameof;
}
