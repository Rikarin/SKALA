using System;

public sealed class Reminders {
    public static void Send() {
        var slots = Array.Empty<string>();
        foreach (var slot in slots) {
            Console.WriteLine(slot);
        }
    }
}
