using System.Collections.Generic;

public sealed class Separators {
    // A list keeps both. Repetition is the shape, not the defect.
    public static readonly List<string> Parts = new() { "-", "-", "-" };
}
