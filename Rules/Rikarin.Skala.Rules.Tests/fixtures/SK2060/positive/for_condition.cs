class C {
    void M() {
        bool more = true;
        for (var i = 0; more = Next(); i++) {
            Step(i);
        }
    }

    static bool Next() => false;

    static void Step(int i) { }
}
