using System;

/// <summary>A set of components stored together.</summary>
public sealed class Archetype : IDisposable {
    /// <summary>Releases the archetype's storage.</summary>
    void IDisposable.Dispose() { }
}
