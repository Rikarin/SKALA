// Nothing above it to inherit from, so the comment is the only statement of what this does.
public sealed class MemoryStore {
    /// <summary>Removes every entry.</summary>
    public void Clear() { }
}
