// ⚠ The "Trojan Source" class: the literal reorders how it reads without changing what it
// contains, so a review sees one string and the program holds another.
// contains: U+202E
namespace Fixtures;

sealed class Paths {
    public const string Upload = "safe‮gnp.exe";
}
