// The ordinary, correct shape: the field being read is initialized first.
static class Defaults {
    public static readonly string Root = "/var/lib";
    public static readonly string Path = Root + "/cache";
}
